<%= form_with(model: course, local: true) do |form| %>
  <% if course.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(course.errors.count, "error") %> prohibited this course from being saved:</h2>

      <ul>
        <% course.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :info_title %>
    <%= form.text_field :info_title %>
  </div>

  <div class="field">
    <%= form.label :info_details %>
    <%= form.text_field :info_details %>
  </div>

  <div class="field">
    <%= form.label :free_info_topics %>
    <%= form.text_field :free_info_topics %>
  </div>

  <div class="field">
    <%= form.label :free_info_details %>
    <%= form.text_field :free_info_details %>
  </div>

  <div class="field">
    <%= form.label :price %>
    <%= form.number_field :price %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
