<%= form_for ([@course, @course.comments.build]) do |f| %>
  <%= f.text_area :content, class:"single-input form-control mb-2", placeholder:"enter comment"%>
  <%= f.submit "send comment", class: "btn btn-light btn-sm shadow-sm border"%>

    <%end%>
